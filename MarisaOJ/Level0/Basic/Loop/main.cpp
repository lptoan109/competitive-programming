#include <bits/stdc++.h>
using namespace std;
int l, r;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> l >> r;
    for(int i = l; i<=r; ++i) cout << i << " ";
    return 0;
}
