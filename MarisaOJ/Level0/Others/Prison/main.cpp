#include <bits/stdc++.h>
using namespace std;
long long x, d, m, y;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> x >> d >> m >> y;
    y += x/356LL;
    x%=365LL;
    for(long long i = 1; i<=x; ++i){
        if()
    }
    return 0;
}
